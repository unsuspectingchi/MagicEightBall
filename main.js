import App from "./app/App.js";

// Unnecessary if loaded from body (needs to be done for ThreeJS integration)--
// just using it because I don't want to change structure
window.onload = () => {
  new App();
};
